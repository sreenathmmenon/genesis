# Genesis

AI Agent Orchestration Platform — describe an outcome, get a deployed multi-agent system.

## Quick start

```bash
./setup.sh   # once, after cloning
./start.sh   # every time
```

Canvas: http://localhost:3000
API docs: http://localhost:8000/docs
